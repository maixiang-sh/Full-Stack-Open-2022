import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css"

// json-severs 启动方法： $ npx json-server --port 3001 --watch db.json

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
